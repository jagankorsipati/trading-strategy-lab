import { useEffect,useState,type ReactNode } from 'react'
import { BarChart3,BookOpen,CandlestickChart,FlaskConical,Gauge,GitCompare,Menu,Moon,ShieldCheck,Sun,TableProperties,X } from 'lucide-react'
import { RouterLink } from './Router'
import { ResearchNotice } from './ui'
const links=[['/','Overview',Gauge],['/strategies','Strategies',ShieldCheck],['/backtests','Backtests',CandlestickChart],['/performance','Performance',BarChart3],['/trades','Trades',TableProperties],['/walk-forward','Walk-forward',FlaskConical],['/execution','Execution',BarChart3],['/comparison','Compare',GitCompare],['/reports','Reports',BookOpen]] as const
export function Layout({path,children}:{path:string;children:ReactNode}){const[open,setOpen]=useState(false);const[dark,setDark]=useState(()=>localStorage.getItem('theme')==='dark');useEffect(()=>{document.documentElement.classList.toggle('dark',dark);localStorage.setItem('theme',dark?'dark':'light')},[dark]);return <div className="app-shell"><aside className={open?'sidebar open':'sidebar'}><div className="brand"><div className="brand-mark">TS</div><div><strong>Trading Strategy Lab</strong><span>Research console</span></div><button className="mobile-close" onClick={()=>setOpen(false)} aria-label="Close navigation"><X/></button></div><nav>{links.map(([to,label,Icon])=><RouterLink key={to} to={to} className={path===to?'active':''}><Icon size={17}/>{label}</RouterLink>)}</nav><div className="sidebar-foot"><span className="status-dot"/>Read-only artifact mode</div></aside><div className="main-column"><header className="topbar"><button onClick={()=>setOpen(true)} className="menu" aria-label="Open navigation"><Menu/></button><div><span className="crumb">Quant Research Dashboard</span><span className="version">v0.1.0</span></div><button onClick={()=>setDark(!dark)} className="theme" aria-label="Toggle color theme">{dark?<Sun/>:<Moon/>}</button></header><main><ResearchNotice/>{children}</main></div></div>}
